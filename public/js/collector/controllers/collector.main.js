angular.module('collector').controller('mainCollectorController', [
  '$scope', '$http', '$window', '$route', '$routeParams',
  function($scope, $http, $window, $route, $routeParams) {
    function init() {

    }

    init();
  }
]);